def somaUm(i:int) : int{
    return i + 1;
}

def quadrado(i : int) : int{
    return i * i;
}

def checkNumPrimo(x : int) : bool{
    var i : int;
    var retorno : bool;

    if(x <= 1){
        write "O número não é primo!\n";
        return false;
    }

    for(i = 2; i <= x/2; i+=1){
        if num
    }
}
